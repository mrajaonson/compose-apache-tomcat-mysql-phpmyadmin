# Docker apache + php + mysql + phpmyadmin

```bash
# Copy .env
cp .env.dist .env

# start
docker-compose up -d

# stop
docker-compose stop

# apache
# http://localhost:8001

# phpmyadmin
# http://localhost:8000
```

